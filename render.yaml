services:
  - type: web
    name: visionnova
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      # Add your Gemini API key in the Render dashboard as GEMINI_API_KEY
